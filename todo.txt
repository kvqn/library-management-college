student database (details of students independent of library)
    library access code (pkey)
    roll no
    name
    branch

book database
    book_id (pkey)
    name
    author
    total_count
    available_count

book lending/returning history
    transaction_id (pkey)
    book_id
    was_borrowed
    student
    date
